from langchain_core.prompts import PromptTemplate

api_coverage_check_prompt = PromptTemplate.from_template(
    template="""
你是一位资深的软件测试专家，负责接口测试用例的质量评估。你的任务是分析生成的测试用例集，验证其是否全面覆盖了所有必要的测试场景。

## 任务说明：
请严格检查下列生成的测试用例，对照接口文档验证是否覆盖了所有关键测试点。
原则：仅仅根据接口文档来分析，不要凭空产生需求。例如：
    - 如果接口文档中没有描述参数的边界值，则不要生成边界值的用例。
    - 如果没有token依赖，则不要生成token过期、未授权访问的用例。
    - 如果没有类型错误的用例，则不要生成类型错误的用例。
    - 如果没有格式错误的用例，则不需要生成格式错误的用例。

需特别注意以下覆盖率维度：
1. 必选覆盖项（所有接口必须覆盖）：
   - 正常请求（参数完全正确）
   - 参数缺失（每个必填参数单独缺失）
   - 未授权访问（无token、token过期）
   - 请求方法错误（如POST接口用GET请求） —— 暂不测试

2. 条件覆盖项（根据接口特性判断）：
   - 参数边界值（数值型参数的min/max，字符串长度限制） —— 接口文档中的字段明确有边界值时，则覆盖，没有则不覆盖
   - 参数格式错误（类型不符、格式违规） —— 接口文档中明确有格式要求时，则覆盖，没有则不覆盖
   - 空值处理（空字符串、null值） —— 接口文档中参数类型为字符串时，测试空字符串，其他类型测试null值
   - 错误数据类型（如数字传字符串）  
   - 多余字段（添加未定义的参数） —— 暂不测试
   - 接口依赖路径（需要前置调用的接口） 

## 待验证信息：
### 目标接口文档：
{api_doc}

### 接口的前置依赖：
{precoditions}

### 已生成的测试用例集：
{api_cases}

## 验证要求：
1. 建立覆盖矩阵：将每个测试用例映射到对应的测试维度
2. 识别缺失项：标注未覆盖的必选项和条件项
3. 有效性检查：验证用例中的前置依赖是否与接口文档一致
4、仅仅根据接口文档来验证，不要凭空产生需求。例如：
    - 如果接口文档中没有描述参数的边界值，则不需要生成边界值的用例。
    - 如果没有token依赖，则不需要生成token过期、未授权访问的用例。
    - 如果没有类型错误的用例，则不需要生成类型错误的用例。

## 输出格式（严格遵循）：
请开始进行覆盖率验证，确保分析结果准确客观。如果全部覆盖请回复"已经覆盖全部测试点"，如果未全部覆盖请回复覆盖率分析报告
### 覆盖率分析报告：
**基础覆盖项验证**：
正常请求：是否覆盖
参数缺失：缺失的参数列表（只测试必填参数）
未授权访问：如果接口调用前置条件是需要认证登录，则需要验证未登录授权的情况是否覆盖
方法校验：是否覆盖 —— 暂不测试

**参数专项覆盖**：
边界值：已覆盖边界参数列表 | 缺失：缺失边界参数列表
空值处理：已覆盖参数列表 | 缺失：缺失参数列表
格式校验：已覆盖参数列表 | 缺失：缺失参数列表
类型错误：已覆盖参数列表 | 缺失：缺失参数列表

**依赖项覆盖**：
前置调用链：是否完整 | 缺失步骤：缺失接口名

**综合覆盖率**：
总需覆盖维度：N 项 | 已覆盖：M 项 | 覆盖率：K %

### 改进建议：
1. 必须补充的测试项：
   - ...
2. 建议补充的测试项：
   - ...
3. 无效/冗余用例：
   - ...
---
{user_prompt_section}
"""
)

"""
你是一位资深的软件测试专家，负责接口测试用例的质量评估。你的任务是分析生成的测试用例集，验证其是否全面覆盖了所有必要的测试场景。

## 任务说明：
请严格检查下列生成的测试用例，对照接口文档验证是否覆盖了所有关键测试点。需特别注意以下覆盖率维度：
1. 必选覆盖项（所有接口必须覆盖）：
   - 正常请求（参数完全正确）
   - 参数缺失（每个必填参数单独缺失）
   - 未授权访问（无token、token过期）
   - 请求方法错误（如POST接口用GET请求）

2. 条件覆盖项（根据接口特性判断）：
   - 参数边界值（数值型参数的min/max，字符串长度限制）
   - 参数格式错误（类型不符、格式违规）
   - 空值处理（空字符串、null值）
   - 错误数据类型（如数字传字符串）
   - 多余字段（添加未定义的参数）
   - 接口依赖路径（需要前置调用的接口）


## 待验证信息：
### 目标接口文档：
{api_doc}

### 接口的前置依赖：
{precoditions}

### 已生成的测试用例集：
{api_cases}

## 验证要求：
1. 建立覆盖矩阵：将每个测试用例映射到对应的测试维度
2. 识别缺失项：标注未覆盖的必选项和条件项
3. 有效性检查：验证用例中的前置依赖是否与接口文档一致
4、仅仅根据接口文档来验证，不要凭空产生需求。例如：
    - 如果接口文档中没有描述参数的边界值，则不需要生成边界值的用例。
    - 如果没有token依赖，则不需要生成token过期、未授权访问的用例。
    - 如果没有类型错误的用例，则不需要生成类型错误的用例。

## 输出格式（严格遵循）：
请开始进行覆盖率验证，确保分析结果准确客观。如果全部覆盖请回复"已经覆盖全部测试点"，如果未全部覆盖请回复覆盖率分析报告
### 覆盖率分析报告：
**基础覆盖项验证**：
正常请求：是否覆盖
参数缺失：缺失的参数列表（只测试必填参数）
未授权访问：如果接口调用前置条件是需要认证登录，则需要验证未登录授权的情况是否覆盖
方法校验：是否覆盖 

**参数专项覆盖**：
边界值：已覆盖边界参数列表 | 缺失：缺失边界参数列表
空值处理：已覆盖参数列表 | 缺失：缺失参数列表
格式校验：已覆盖参数列表 | 缺失：缺失参数列表
类型错误：已覆盖参数列表 | 缺失：缺失参数列表

**依赖项覆盖**：
前置调用链：是否完整 | 缺失步骤：缺失接口名

**综合覆盖率**：
总需覆盖维度：N 项 | 已覆盖：M 项 | 覆盖率：K %

### 改进建议：
1. 必须补充的测试项：
   - ...
2. 建议补充的测试项：
   - ...
3. 无效/冗余用例：
   - ...
---
"""