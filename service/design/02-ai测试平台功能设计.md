## 一、系统概述

AI 测试平台是一个集成了项目管理、测试环境管理、知识库管理、功能测试、接口测试、测试执行和 AI 辅助测试等功能的综合性测试平台。该平台旨在提高测试效率，实现测试自动化，并通过 AI 技术辅助测试用例生成、预执行验证与正式执行。

## 二、系统架构

### 1、系统模块设计

1. 用户模块
2. 项目模块
3. 测试环境模块
4. **知识库管理模块**
5. 功能测试模块
6. 接口测试模块
7. 测试管理模块
8. 测试执行模块
9. AI 用例生成模块
10. WEB 测试模块（后续扩展）

### 2. 模块间交互关系

- 用户模块 → 项目模块：用户创建和管理项目
- 项目模块 → 测试环境模块：项目关联多个测试环境
- 项目模块 → **知识库管理模块**：项目关联独立 RAG workspace
- **知识库管理模块** → 功能测试模块 / 接口测试模块：提供需求与接口文档检索
- 项目模块 → 功能测试模块：项目包含多个需求和功能用例
- 项目模块 → 接口测试模块：项目包含多个接口和 API 用例
- 功能测试模块 → 测试管理模块：功能用例组织到测试套件
- 接口测试模块 → 测试管理模块：API 用例组织到测试套件
- 测试管理模块 → 测试执行模块：执行测试任务和套件
- 接口测试模块 / 功能测试模块 → AI 用例生成模块：基于文档生成用例
- AI 用例生成模块 → 接口测试模块 / 功能测试模块：生成结果入库

### 3. 用户角色与权限

- 超级管理员：系统全局管理权限
- 项目所有者：项目全部权限
- 项目编辑者：项目资源编辑权限
- 项目查看者：项目资源查看权限

## 三、功能模块设计

### 1. 用户模块

#### 1.1 功能描述

实现用户账号管理、登录认证和权限控制。

#### 1.2 核心功能

- 用户注册与登录
- 用户权限管理
- 超级管理员功能
- 账号状态管理

#### 1.3 数据模型

基于用户表（user）设计，包含用户基本信息、密码哈希、权限标识等。

### 2. 项目模块

#### 2.1 功能描述

管理测试项目，控制项目成员权限，组织项目内的测试资源。

#### 2.2 核心功能

- 项目创建与管理
- 项目成员管理与权限控制
- 项目模块划分

#### 2.3 数据模型

基于项目表（project）、项目成员表（project_member）和项目模块表（project_module）设计，实现项目的组织结构管理。`project_module` 在同一项目下模块名唯一，**仅支持一级模块**（无 parent_id）。

#### 2.4 API 清单 — 项目模块

路由前缀：`/api/v1/projects/{project_id}/modules`；权限：viewer 可读，editor+ 可写。

| Method | Path | 说明 |
|--------|------|------|
| GET | `/modules` | 模块列表（分页，默认 20） |
| POST | `/modules` | 新增模块 |
| PATCH | `/modules/{module_id}` | 编辑模块 |
| DELETE | `/modules/{module_id}` | 删除模块（有关联 requirement/api/knowledge 时 409） |

### 3. 测试环境模块

#### 3.1 功能描述

管理不同测试环境的配置，支持多环境测试，并为 TestApiEngine 提供可复现的环境快照。

#### 3.2 核心功能

- 测试环境配置管理（base_url、headers、envs）
- 环境变量目录（env_catalog）与变量文件（test_environment）
- 数据库连接配置（db_connection + environment_db_relation，全局池化）
- **全局工具函数**（env_function_file + environment_function_relation，按 sort_order 拼接 global_func）
- **环境快照**（test_environment_snapshot）：整包 test_env_data，供 AI 预执行与正式执行复现
- **上传文件**（env_uploaded_file）：项目级隔离，供用例 request.files 引用 uploaded_file_id
- **调试运行变量**（debug_runtime_var）：按 environment_id 隔离，merge_debug 时覆盖 envs；不写入快照

#### 3.3 数据模型

基于以下表设计（详见 [03-ai测试平台数据库设计.md](./03-ai测试平台数据库设计.md) v2.2）：

| 表名 | 职责 |
|------|------|
| env_catalog | 项目内环境目录树 |
| test_environment | 变量文件（环境实例） |
| test_environment_config | base / headers / envs 配置项（config_type 含 secret） |
| db_connection | 数据库连接定义（全局池化，connection_name 唯一） |
| environment_db_relation | 变量文件 ↔ DB 连接 M:N |
| env_function_file | 函数文件（file_name 全局唯一，source_code） |
| environment_function_relation | 变量文件 ↔ 函数文件 M:N（sort_order） |
| env_uploaded_file | 项目上传文件（storage_key 本地/OSS） |
| debug_runtime_var | 调试变量（source=manual \| engine） |
| test_environment_snapshot | 整包 payload JSON（version，is_active） |

**test_env_data 与表的映射**

| test_env_data 字段 | 存储位置 |
|--------------------|----------|
| base_url | test_environment_config（group=base） |
| headers | test_environment_config（group=headers） |
| envs | test_environment_config（group=envs）+ debug_runtime_var（merge_debug=true 时） |
| global_func | env_function_file 经 environment_function_relation 按 sort_order 拼接 |
| db | db_connection 经 environment_db_relation |
| 整包快照 | test_environment_snapshot.payload（不含 debug var） |

#### 3.4 API 清单（Phase 1 / 2 / 3）

路由前缀：`/api/v1/env/*`；统一响应 `{ code, message, data }`。

**Phase 1 — 环境 / 配置 / DB / 快照**

| Method | Path | 说明 |
|--------|------|------|
| GET/POST | `/catalogs` | 目录树 CRUD |
| GET/POST/PATCH/DELETE | `/environments` | 变量文件 CRUD |
| GET/PUT/POST/PATCH/DELETE | `/environments/{id}/configs/*` | 配置项管理 |
| GET/POST/PATCH/DELETE | `/db-connections` | DB 连接 CRUD |
| PUT | `/environments/{id}/db-connections` | 绑定 DB |
| POST/GET | `/environments/{id}/snapshots` | 快照创建 / 列表 |
| GET | `/environments/{id}/test-env-data` | 组装 test_env_data（密码脱敏；use_snapshot / merge_debug） |

**Phase 2 — 函数文件 / 导入导出**

| Method | Path | 说明 |
|--------|------|------|
| GET/POST/PATCH/DELETE | `/function-files` | 函数文件 CRUD |
| POST | `/function-files/validate` | 源码 compile 语法校验 |
| PUT | `/environments/{id}/function-files` | 全量绑定函数 |
| GET | `/environments/{id}/export` | 导出 JSON bundle（含 embed 函数/DB 定义） |
| POST | `/environments/import` | JSON body 导入（import_mode: reference \| embed） |
| POST | `/environments/import-file` | multipart JSON 文件导入 |

**Phase 3 — 上传文件 / 调试变量**

| Method | Path | 说明 |
|--------|------|------|
| GET/POST | `/uploaded-files` | 上传文件列表 / 上传（MAX_UPLOAD_BYTES 限制） |
| DELETE | `/uploaded-files/{id}` | 软删除 |
| GET | `/uploaded-files/{id}/download` | 下载 |
| GET | `/environments/{id}/debug-vars` | 调试变量列表 |
| PUT | `/environments/{id}/debug-vars` | 批量 upsert（source=manual） |
| POST | `/environments/{id}/debug-vars/sync` | 引擎回写（source=engine） |
| DELETE | `/debug-vars/{id}` | 删除单键 |

**引擎集成**

- `TestEnvDataAssembler`：assemble / merge_debug_vars / get_test_env_data
- `service/test_execution/env_loader.py`：明文加载 + case_payload 文件 resolve
- workflow / MCP `load_evn_data(environment_id)`：environment_id>0 读平台，否则硬编码 fallback

### 4. 知识库管理模块

#### 4.1 功能描述

按项目隔离管理需求文档与接口文档，为 RAG 检索和 AI 用例生成提供知识来源。

#### 4.2 核心功能

- 项目级知识库 workspace 管理（与 `rag/rag_storage/{project}/` 对应）
- 需求文档上传与索引（Markdown、PDF 等，经 LightRAG / RAGAnything 处理）
- 接口文档上传与索引（Swagger/OpenAPI JSON、Markdown 说明）
- 文档检索（功能需求检索、接口文档流式检索）
- 文档版本与重新索引

#### 4.3 实现方式

| 能力 | 实现 |
|------|------|
| 需求/接口 RAG | **RagGateway**：LightRAG 服务可用 → `RAGClient`；否则 → `RAGManager` |
| Agent 工具 | `search_requirement`、`search_api_document`（Phase D 改造） |
| 文档解析 | Swagger/OpenAPI → `utils/parser/`；AI → RAGAnything / LightRAG |

#### 4.4 数据模型

| 表名 | 职责 |
|------|------|
| knowledge_workspace | 项目级 RAG workspace（requirement / api） |
| knowledge_document | **逻辑文档**（title、project、doc_type、parse_mode 不可变） |
| knowledge_document_version | **版本记录**（文件、索引/解析状态、RAG doc id） |

**与业务表关系**

- 需求 AI 索引成功 → **upsert** `requirement_candidate`（用户 confirm 后写入 `requirement_doc`）
- 接口 Swagger/OpenAPI 解析 → 结果暂存 version；**手动** import 到 `api_interface.module_id`
- 删除知识库 → 不删除已 sync 的 requirement / api_interface（FK SET NULL）

**Phase 进度**

- Phase A：DB 迁移 + 项目 module CRUD + ORM（已完成）
- Phase B：知识库 CRUD / 分页 / 权限（已完成）
- Phase C：异步索引 + RagGateway（已完成）
- Phase D：import-interfaces + MCP 改造（已完成）

#### 4.5 API 清单（规划）

路由前缀：`/api/v1/knowledge/*`；详见实施计划文档。

| Method | Path | 说明 |
|--------|------|------|
| POST | `/documents?project_id=` | 上传 |
| GET | `/documents` | 列表（分页） |
| GET | `/documents/{id}` | 详情 |
| POST | `/documents/{id}/versions` | 重传 |
| GET | `/documents/{id}/versions` | 历史版本 |
| GET | `/documents/{id}/download` | 下载当前版 |
| POST | `/documents/{id}/reindex` | 重新索引 |
| DELETE | `/documents/{id}` | 删除 |
| POST | `/documents/{id}/versions/{vid}/import-interfaces` | 导入接口到 module |

### 5. 功能测试模块

#### 5.1 功能描述

管理需求文档和功能测试用例，支持知识库索引候选确认、AI 辅助生成与纯手工维护。

#### 5.2 核心功能

- **需求管理**（两种方式）
  - 方式一：知识库 requirement 文档索引 → `requirement_candidate` 待确认 Tab → 用户 confirm → `requirement_doc`
  - 方式二：手工新增 `requirement_doc`（`source_type=manual`，module 必填）
- **待确认候选**：列表 / 数量 badge / 详情预览 / confirm（同版本 409）/ cancel
- **用例目录**：`functional_case_catalog` 三级树
- **功能用例**：纯手工新建、复制、排序、批量编辑/删除、CSV 导出
- **AI 生成**：会话预览 `output_payload` → 勾选保存 → `functional_test_point` + `functional_case`
- **测试点管理**（AI 中间产物，详情展示）
- 用例执行结果 `exec_result` 与套件删除 409 保护

#### 5.3 数据模型

- `requirement_candidate`（知识库索引产出，待用户确认）
- `requirement_doc`（含 `source_type`、同项目 title 唯一）
- `functional_case_catalog`
- `functional_test_point`
- `functional_case`（含 `catalog_id`、`sort_order`、`exec_result`、`jira_issue_key`）

**知识库 → 需求链路**

```
knowledge index → requirement_candidate → confirm API → requirement_doc
```

**AI 生成链路**

```
requirement_doc → ai_generation_session(output_payload) → functional_test_point → functional_case
```

### 6. 接口测试模块

#### 6.1 功能描述

管理 API 接口定义、依赖关系和测试用例，支持接口自动化测试。

#### 6.2 核心功能

- 接口定义管理（Swagger/OpenAPI 导入、RAG 同步）
- 接口依赖关系管理
- 基础用例管理（自然语言步骤 + 预期）
- API 可执行用例管理（结构化 + 预执行状态）

#### 6.3 数据模型

基于以下表设计（v2）：

- api_interface（含 project_id、module_id、source、version）
- api_dependency_group、api_dependency（from_api_id / to_api_id 为 INT FK）
- api_base_case（含 dependencies JSON，对齐 BaseCaseModel）
- api_test_case（**case_payload JSON** + title + review_status + exec_status + generation_count）

**三层用例模型**

| 层级 | 表 | 代码模型 | 说明 |
|------|-----|----------|------|
| 接口定义 | api_interface | APIDocumentParserModel | path/method/parameters/responses |
| 基础用例 | api_base_case | BaseCaseModel | name/steps/dependencies/expected |
| 可执行用例 | api_test_case | APIruncaseModel → case_payload | TestApiEngine 可直接执行 |

### 7. 测试管理模块

#### 7.1 功能描述

组织和管理测试任务和测试套件，实现测试的系统化管理。

#### 7.2 核心功能

- 测试任务管理（含 project_id）
- 测试套件管理（含 project_id）
- 用例组织与排序（**支持 API 与功能用例多态**）

#### 7.3 数据模型

- test_task（type：api / functional / ui）
- test_suite（type：api / functional / ui）
- **suite_case_relation**（case_type：api / functional + case_id + case_order）
- task_suite_relation

### 8. 测试执行模块

#### 8.1 功能描述

执行测试用例，记录测试结果，生成测试报告。区分 **API 自动化执行** 与 **功能用例执行** 两种模式。

#### 8.2 核心功能

- **API 用例执行**（TestApiEngine）：单用例调试、套件执行、任务执行
- **功能用例执行**：当前以手工执行 + 结果回填为主；后续扩展 UI 自动化
- 执行结果记录（含环境快照、用例快照）
- 测试报告生成与查看

#### 8.3 API 自动化执行

**执行引擎**：TestApiEngine（`TestApiEngineXin/ApiEngine/core.py` → `TestRunner`）

**流程**

1. 从 test_task / test_suite 解析用例列表（suite_case_relation，case_type=api）
2. 加载 test_environment_snapshot.payload 作为 test_env_data
3. 从 api_test_case 读取 case_payload（exec_status=ready）
4. 调用 TestRunner.execute_cases()
5. 写入 api_case_run_record（含 case_snapshot、status、duration_ms、log_data）

**与 AI 预执行的区别**

| 项目 | AI 预执行 | 平台正式执行 |
|------|-----------|--------------|
| 触发 | api_runcase_workflow | test_execution 服务 |
| 目的 | 验证生成质量 | 回归/任务执行 |
| 状态字段 | review_status | exec_status + 运行记录 |
| 环境 | 当前 test_env_data | env_snapshot_id 固定快照 |

#### 8.4 功能用例执行

当前阶段：

- 测试人员在平台查看 functional_case（test_steps / expected_result）
- 手工执行后填写 actual_result，更新 status
- 功能用例可加入 test_suite（case_type=functional），任务执行时生成**手工执行清单**而非自动跑

后续扩展（Phase4 / WEB 测试模块）：

- UI 自动化引擎对接
- functional_case_run_record 表（规划）

#### 8.5 测试报告（最小设计）

**报告粒度**

| 级别 | 数据来源 | 内容 |
|------|----------|------|
| 用例级 | api_case_run_record | 单用例 status、请求/响应、日志、耗时 |
| 套件级 | test_suite_run | 通过/失败/异常/跳过统计、用例明细列表 |
| 任务级 | test_task_run | 多套件汇总、总通过率、执行时间线 |

**报告字段（任务级汇总示例）**

```json
{
  "task_run_id": 1,
  "task_name": "登录模块回归",
  "environment": "test",
  "start_time": "...",
  "duration_ms": 120000,
  "summary": {"total": 50, "passed": 45, "failed": 3, "error": 2, "skipped": 0},
  "pass_rate": 0.9,
  "suite_results": [...]
}
```

**API（规划）**

```plain
GET /api/v1/test-runs/tasks/{id}/report      # 任务报告
GET /api/v1/test-runs/suites/{id}/report      # 套件报告
GET /api/v1/test-runs/cases/{id}              # 单用例详情
```

报告可由 task_run / suite_run / case_run 聚合生成，无需单独大表；可选增加 test_report 表存 PDF/HTML 导出快照。

#### 8.6 数据模型

- api_case_run_record（含 status、environment_id、env_snapshot_id、case_snapshot、duration_ms、log_data）
- test_suite_run（含 error_cases、triggered_by、env_snapshot_id）
- test_task_run（含 error_cases、triggered_by、env_snapshot_id）

### 9. AI 用例生成模块

#### 9.1 功能描述

利用 AI 技术，结合 RAG 知识库，根据需求文档和接口定义自动生成测试用例，并通过 TestApiEngine 预执行验证 API 用例可运行性。

#### 9.2 核心功能

- 基于需求文档生成功能测试点与功能用例
- 基于接口文档生成基础 API 用例与可执行结构化用例
- 覆盖率验证与测试点/用例补全
- **可执行用例预执行验证**（TestRunner）
- 失败自动重试（generation_count，上限 MAX_GENERATOR_COUNT）
- 用例审核（review_status → exec_status）
- 生成会话审计与追溯

#### 9.3 功能用例 AI 生成流程

```
1. 用户通过 Agent 或 API 发起请求（指定 project、module、需求描述）
2. search_requirement：RAG 检索需求文档
3. case_generator_workflow：
   a. 生成测试点 → 写入 functional_test_point（规划）
   b. 覆盖率验证 → 补全测试点
   c. 生成测试用例 → 写入 functional_case
4. 创建 ai_generation_session 记录（gen_type=functional）
5. 返回用例列表，source=ai
```

#### 9.4 API 用例 AI 生成流程

```
1. 用户选择目标接口或描述接口模块
2. search_api_document：RAG 检索接口文档
3. load_evn_data：加载 test_env_data（来自 test_environment_snapshot）
4. api_document_to_cases → APICaseGeneratorMainWorkflow：
   a. api_basecase_workflow：生成基础用例 → api_base_case
   b. api_runcase_workflow（并发）：基础用例 → 结构化可执行用例
   c. TestRunner 预执行 → review_status（success/fail/error）
   d. 失败则 re_structure_runcase_generator 重试（≤ MAX_GENERATOR_COUNT）
   e. 入库 api_test_case（case_payload + review_status + generation_count）
5. 创建 ai_generation_session 记录（gen_type=api_runnable）
6. 人工审核：review_status=success 且人工确认 → exec_status=ready
7. 加入 test_suite → 正式执行
```

#### 9.5 用例状态机

**API 可执行用例（api_test_case）**

| 字段 | 状态 | 含义 |
|------|------|------|
| review_status | init / success / fail / error | AI 预执行结果 |
| exec_status | pending / ready / disabled | 是否允许加入套件正式执行 |

状态流转：

```
生成 → review_status=init
     → 预执行 → success|fail|error
     → 人工审核通过 → exec_status=ready
     → 禁用 → exec_status=disabled
```

#### 9.6 数据模型

生成结果写入各用例表，审计信息写入 **ai_generation_session**（v2，详见 03 文档）：

- ai_generation_session（gen_type、input_ref、model_name、prompt_hash、status）
- functional_test_point.generation_session_id
- functional_case.generation_session_id
- api_base_case.generation_session_id
- api_test_case.generation_session_id

#### 9.7 实现方式（当前代码目录）

| 组件 | 路径 |
|------|------|
| Supervisor + 子 Agent | agents/case_generate_agent.py |
| 双记忆 | agents/memory/manager.py |
| 功能用例工作流 | workflow/case_generator_workflow.py |
| API 基础/可执行/主工作流 | workflow/api_basecase_workflow.py、api_runcase_workflow.py、api_case_main_workflow.py |
| Agent 工具 | mcp_tools/tools.py |
| 入口示例 | main.py |

### 10. WEB 测试模块（后续扩展）

该模块将在后续扩展讲解，暂不定义具体功能和数据结构。与功能用例执行、UI 自动化引擎对接。

## 四、系统核心流程设计

### 1. 测试项目创建流程

1. 用户登录系统
2. 创建新项目，设置项目基本信息
3. 添加项目成员，分配权限
4. 创建项目模块
5. 配置测试环境（含 snapshot）
6. **上传需求/接口文档至知识库，完成 RAG 索引**

### 2. 功能测试用例流程

1. 创建或导入需求文档（requirement_doc + 知识库索引）
2. AI 或手工生成功能测试点（functional_test_point）
3. AI 或手工生成功能用例（functional_case）
4. 管理测试用例，加入测试套件
5. 手工执行并回填结果（或后续 UI 自动化）

### 3. 接口测试流程

1. 定义或导入接口信息（api_interface + 知识库索引）
2. 设置接口依赖关系（api_base_case.dependencies 或 api_dependency_group）
3. AI 生成 API 基础用例与可执行用例，TestApiEngine 预执行验证
4. 人工审核（exec_status=ready）
5. 组织 API 测试套件
6. 正式执行接口测试
7. 记录测试结果，生成报告

### 4. AI 辅助 API 测试用例生成流程

1. 选择目标接口（或自然语言描述）
2. RAG 检索接口文档，加载测试环境 snapshot
3. LangGraph 工作流：基础用例 → 可执行用例 → 预执行 → 重试
4. 写入 api_test_case，关联 ai_generation_session
5. 人工审核和优化（review_status / exec_status）
6. 将用例添加到测试套件
7. 执行测试并查看报告

## 五、后端技术栈实现

- 编程语言：Python
- Web 框架：FastAPI（业务 API，Phase2）
- 数据库：MySQL 8.0
- ORM 模型：Tortoise ORM
- AI 编排：LangGraph + LangChain
- RAG：LightRAG / RAGAnything
- API 执行引擎：TestApiEngine（TestApiEngineXin）
- MySQL 异步驱动：asyncmy / aiomysql

## 六、与数据库设计文档的对应关系

本功能设计已与 [03-ai测试平台数据库设计.md](./03-ai测试平台数据库设计.md) **v2.1** 对齐，主要变更包括：

- 新增 **knowledge_workspace**、**knowledge_document**（§四 知识库模块）
- requirement_doc / api_interface 增加 **source_document_id**、索引状态字段
- 新增 functional_test_point、test_environment_snapshot、ai_generation_session
- api_test_case 采用 case_payload + review_status / exec_status
- suite_case_relation 支持 api / functional 多态
- test_task / test_suite 增加 project_id 与 functional 类型
- 运行记录增加 case_snapshot、env_snapshot_id、error_cases
- ai_generation_session 增加 knowledge_document_id

### 6.1 接口测试管理模块（`/api/v1/api-test`）

- **目录**：树 CRUD、移动（最大 5 级）、强删子树及接口
- **导入**：`imports/preview` + `imports/confirm`（skip/upsert；新建才 infer 依赖）
- **接口**：CRUD、复制（path `/_copyNN`）、排序、仅展示 `is_current`
- **Tab1 调试**：debug-template CRUD、fill-from-doc、debug-run → `RunnerGateway`
- **Tab2 依赖**：doc-preview、dependencies CRUD、reanalyze（manual 边保留）
- **Tab3 用例**：generate-preview → confirm（拆分 basecase/runcase workflow）、case CRUD/debug、run-records

详见 [03-ai测试平台数据库设计.md](./03-ai测试平台数据库设计.md) §六 与 `service/api_test/` 分包实现。

架构与引擎集成见 [01-ai智能测试平台系统架构设计.md](./01-ai智能测试平台系统架构设计.md)。
