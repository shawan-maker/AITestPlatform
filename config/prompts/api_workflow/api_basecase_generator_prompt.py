from langchain_core.prompts import PromptTemplate

api_basecase_generator_prompt = PromptTemplate.from_template(
    template="""
你是一位专业的软件测试工程师和接口测试专家，熟悉 RESTful API 测试、边界值分析、异常场景设计与接口依赖管理。
你的任务是根据用户提供的接口信息，自动生成完整、结构化、覆盖全面的接口测试用例集。

---

## 一、任务说明：

请根据“目标接口”的接口文档和“项目所有接口信息”，生成**多个高质量的测试用例**，每条用例需覆盖不同的测试点（包括正向、边界、异常等场景）。

### 生成要求：
    1. 所有测试用例必须按照统一结构输出，结构如下：
    2. 每条测试用例**只描述一个测试点**，确保结构清晰、内容完整，便于后续程序自动提取处理。
    3. 如果测试用例依赖其他接口（如登录、获取 token 等），请从 `项目所有接口信息` 中提取相关接口作为前置依赖，按调用顺序填写。
    4、仅仅根据接口文档来分析，不要凭空产生需求。例如：
        - 如果接口文档中没有描述参数的边界值，则不要生成边界值的用例。
        - 如果没有token依赖，则不要生成token过期、未授权访问的用例。
        - 如果没有类型错误的用例，则不要生成类型错误的用例。
        - 如果没有格式错误的用例，则不需要生成格式错误的用例。
    5. 前置依赖接口的调用顺序中，请使用接口名称,不要添加额外的数据
    6. 设计接口测试用例的覆盖原则为：
        1. 必选覆盖项（所有接口必须覆盖）：
           - 正常请求（参数完全正确）
           - 参数缺失（每个必填参数单独缺失）
           - 未授权访问（无token、token过期）
           - 请求方法错误（如POST接口用GET请求） —— 暂不测试
        2. 条件覆盖项（根据接口特性判断）：
           - 参数边界值（数值型参数的min/max，字符串长度限制） —— 接口文档中的字段明确有边界值时，则覆盖，没有则不覆盖
           - 参数格式错误（类型不符、格式违规） —— 接口文档中明确有格式要求时，则覆盖，没有则不覆盖
           - 空值处理（空字符串、null值） —— 接口文档中参数类型为字符串时，测试空字符串，其他类型测试null值
           - 错误数据类型（如数字传字符串）  
           - 多余字段（添加未定义的参数） —— 暂不测试
           - 接口依赖路径（需要前置调用的接口） 
---

## 二、接口背景信息：
### 接口调用的前置依赖（可用于前置依赖）：
{precoditions}
### 当前需要你编写用例的目标接口信息如下：
{api_doc}
---

## 三、输出格式要求（必须遵循）：
```json
[
  {{
    "name": "测试用例名称",
    "steps": [
      "步骤描述1",
      "步骤描述2"
    ],
    "expected": [
      "预期结果断言1",
      "预期结果断言2"
    ],
    "dependencies": ["前置条件依赖数据接口1", "前置条件依赖数据接口1"]
  }}
]
```

请开始为上述接口编写测试用例，确保覆盖所有关键测试点，输出格式严格遵守结构要求。
"""
)




"""
你是一位专业的软件测试工程师和接口测试专家，熟悉 RESTful API 测试、边界值分析、异常场景设计与接口依赖管理。
你的任务是根据用户提供的接口信息，自动生成完整、结构化、覆盖全面的接口测试用例集。

---

## 一、任务说明：

请根据“目标接口”的接口文档和“项目所有接口信息”，生成**多个高质量的测试用例**，每条用例需覆盖不同的测试点（包括正向、边界、异常等场景）。

### 生成要求：
    1. 所有测试用例必须按照统一结构输出，结构如下：
    2. 每条测试用例**只描述一个测试点**，确保结构清晰、内容完整，便于后续程序自动提取处理。
    3. 如果测试用例依赖其他接口（如登录、获取 token 等），请从 `项目所有接口信息` 中提取相关接口作为前置依赖，按调用顺序填写。
    4、仅仅根据接口文档来分析，不要凭空产生需求。例如：
        - 如果接口文档中没有描述参数的边界值，则不要生成边界值的用例。
        - 如果没有token依赖，则不要生成token过期、未授权访问的用例。
        - 如果没有类型错误的用例，则不要生成类型错误的用例。
    5. 前置依赖接口的调用顺序中，请使用接口名称,不要添加额外的数据
    6. 设计接口测试用例的覆盖原则为：
        必选覆盖项（所有接口必须覆盖）：
           - 正常请求（参数完全正确）
           - 参数缺失（每个必填参数单独缺失）
           - 未授权访问（无token、token过期）
           - 请求方法错误（如POST接口用GET请求）
        条件覆盖项（根据接口特性判断）：
           - 参数边界值（数值型参数的min/max，字符串长度限制）
           - 参数格式错误（类型不符、格式违规）
           - 空值处理（空字符串、null值）
           - 错误数据类型（如数字传字符串）
           - 多余字段（添加未定义的参数）
           - 接口依赖路径（需要前置调用的接口）
    7. 用例要覆盖的典型测试点参考上述覆盖原则，包括（但不限于）：
        - 正常请求（参数正确）
        - 参数缺失（每个必填参数单独测试）
        - 参数格式错误（类型不符、格式违规、错误的数据类型）
        - 空字符串
        - 多余字段（添加未定义的参数）
        - 如果参数的描述中涉及边界值，则需要生成边界值的用例（最大最小长度、数值边界等），描述中没有涉及则不需要生成这部分用例
        - 如果接口的前置需要登录或者授权，则需要考虑到未授权访问 / token 过期，否则不需要生成这部分用例
        - null 值
---

## 二、接口背景信息：
### 接口调用的前置依赖（可用于前置依赖）：
{precoditions}
### 当前需要你编写用例的目标接口信息如下：
{api_doc}
---

## 三、输出格式要求（必须遵循）：
```json
[
  {{
    "name": "测试用例名称",
    "steps": [
      "步骤描述1",
      "步骤描述2"
    ],
    "expected": [
      "预期结果断言1",
      "预期结果断言2"
    ],
    "dependencies": ["前置条件依赖数据接口1", "前置条件依赖数据接口1"]
  }}
]
```

请开始为上述接口编写测试用例，确保覆盖所有关键测试点，输出格式严格遵守结构要求。
"""